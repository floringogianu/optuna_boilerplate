[lint]
select = [
  "E",   # pycodestyle
  "F",   # Pyflakes
  "UP",  # pyupgrade
  "B",   # flake8-bugbear 
  "SIM", # flake8-simplify 
  "I",   # isort
]
ignore = [ 
  "UP032",  # use f-string instead of `format` call
]